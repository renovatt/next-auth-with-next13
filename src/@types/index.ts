export type RegisterUserProps = {
    name: string;
    email: string;
    password: string;
}

export type InputProps = {
    name: string;
    label: string;
    type: string;
    placeholder: string;
}